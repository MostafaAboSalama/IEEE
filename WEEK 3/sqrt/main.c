#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    double n,res;
    printf("Enter the number : ");
    scanf("%lf",&n);
    res= sqrt(n);
        printf("The result = %lf", res);

    return 0;
}
